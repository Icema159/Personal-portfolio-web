// Enable each effect independently during rollout.
export const effectsConfig = Object.freeze({
  smokeEnabled: true,
  distortionEnabled: true,
  resolution: 384,
  pressureIterations: 16,
  radius: 0.018,
  decay: 1.0,
});
